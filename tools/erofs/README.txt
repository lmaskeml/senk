EROFS vendor ayiklama (Android 15 / crDroid)

Tek klasor: bu dizin (tools\erofs).
Ic ice tools\erofs\erofs KULLANMA.

ZIP: sekaiacg/erofs-utils Cygwin_x86_64
Kopyala: extract.erofs.exe + cygwin1.dll (+ diger .dll)

Komut:
  extract.erofs.exe -i vendor.img -x -o .\out
