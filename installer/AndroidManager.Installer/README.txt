Android Manager installer scaffold.

Primary distribution today: run ..\build.ps1 to produce a win-x64 zip under /dist.

Expand this WiX project to harvest the publish folder when WiX Toolset is available in CI.
